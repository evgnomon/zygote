---
- name: Add shards
  add_host:
    name: "{{ item }}"
    groups: shards
    ansible_user: "root"
  loop:
    - shard-a.{{ z_domain }}
    - shard-b.{{ z_domain }}
    - shard-c.{{ z_domain }}
  tags:
    - always
