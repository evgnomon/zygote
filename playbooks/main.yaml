---
- name: Main Playbook
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Run check.sh"
      shell: . playbooks/files/check.sh
      args:
        chdir: "{{ lookup('env', 'PWD') }}"

- name: Setup the controller VM
  hosts: controller
  gather_facts: false
  tasks:
    - debug:
        msg: "{{ ansible_facts['distribution'] }}"
  tags:
    - controller
